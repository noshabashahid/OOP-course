Here’s a simple program that demonstrates OOP using a Car class:
#include <iostream>
using namespace std;

// Defining a class named Car
class Car {
private:
    string brand;
    int year;

public:
    // Constructor to initialize attributes
    Car(string b, int y) {
        brand = b;
        year = y;
    }

    // Method to display car details
    void display() {
        cout << "Brand: " << brand << "\n";
        cout << "Year: " << year << "\n";
    }
};

int main() {
    // Creating an object of Car
    Car car1("Toyota", 2022);
    car1.display();
    return 0;
}
